//uzycie instrukcji wyboru
#include <iostream>
#include <cctype>
using namespace std;

int main()
{
  int a,b;
  cout << "Podaj dwie liczby calkowite: ";
  cin >> a >> b;
  int c = a > b ? a : b; //c=a jesli a > b, inaczej c=b 
}
